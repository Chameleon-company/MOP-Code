function getColor(index, type = 'primary') {
    const primaryColors = [
        '#B5EAEA',
        '#CDF0EA',
        '#CAF7E3',
        '#EDFFEC',
    ]
    switch (type) {
        default: return primaryColors[index]
    }
}