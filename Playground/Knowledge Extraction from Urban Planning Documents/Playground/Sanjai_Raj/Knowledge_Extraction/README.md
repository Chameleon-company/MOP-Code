# Urban Planning Knowledge Extraction System

## Overview
Automated system to extract Zoning, Height, and Parking data from Victorian planning PDFs.

## How to Run
1. Install dependencies: `pip install -r requirements.txt`
2. Run pipeline: `python -m src.main`

## Features
- Regex-based attribute extraction.
- Professional logging and data quality reporting.